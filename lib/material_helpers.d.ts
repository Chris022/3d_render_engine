import { Material } from "./types";
export declare let OutlineMaterial: (edge_color: string, fill_color: string, line_width: number) => Material;
export declare let OutlineMaterialGlow: (edge_color: string, fill_color: string, line_width: number, glow_stength: number) => Material;
